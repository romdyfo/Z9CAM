import 'package:flutter/material.dart';

class StyrofoamPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('스티로폼류'),
        backgroundColor: const Color(0xff6AC99F),
      ),
      body: Center(
        child: Text('스티로폼류 페이지'),
      ),
    );
  }
}
